<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.shmashine.api.dao.BizFileDao">
    <!-- 删除文件 -->
    <delete id="deleteFile">
        delete
        from tbl_sys_file
        where v_business_id = #{businessId}
          and i_business_type = #{businessType}
    </delete>
    <delete id="deleteFileAll">
        delete
        from tbl_sys_file
        where v_business_id = #{businessId}
          and i_business_type = #{businessType}
    </delete>

    <!-- 获取电梯图片 -->
    <select id="getElevatorImg" resultType="java.util.Map">
        select *
        from tbl_sys_file
        where v_business_id = #{businessId}
          and i_business_type = #{businessType}
    </select>
</mapper>