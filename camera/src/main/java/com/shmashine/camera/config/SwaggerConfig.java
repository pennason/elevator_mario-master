package com.shmashine.camera.config;

/*@Configuration
// @EnableSwagger2
public class SwaggerConfig {
    @Bean
    public Docket createRestApi() {
        return new Docket(DocumentationType.SWAGGER_2)
                .pathMapping("/")
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.shmashine.camera.controller"))
                .paths(PathSelectors.any())
                .build().apiInfo(new ApiInfoBuilder()
                        .title("摄像头服务")
                        .description("摄像头服务 API接口文档 & 测试接口")
                        .version("1.0.0")
                        .build());

    }
}*/
